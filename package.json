{
  "name": "cafe-time-period-timelapse",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "A 3D café interior that transforms across time periods (1945-2055). Built with Vite, TypeScript, and Three.js.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run"
  },
  "dependencies": {
    "three": "^0.185.1"
  },
  "devDependencies": {
    "@types/three": "^0.185.4",
    "typescript": "^7.0.2",
    "vite": "^8.2.2",
    "vitest": "^4.1.11"
  }
}
