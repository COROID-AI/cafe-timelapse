{
  "name": "cafe-time-period-timelapse",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "check:eras": "tsx src/scripts/checkEras.ts",
    "check:scene": "tsx src/scripts/checkScene.ts",
    "check": "npm run check:eras && npm run check:scene && npm run typecheck"
  },
  "dependencies": {
    "three": "^0.185.1"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "@types/three": "^0.185.1",
    "tsx": "^4.23.1",
    "typescript": "~5.9.3",
    "vite": "^8.2.0"
  }
}
