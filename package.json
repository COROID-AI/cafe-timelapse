{
  "name": "cafe-timelapse",
  "version": "0.1.0",
  "description": "A 3D time-period timelapse of a café interior built with Three.js and Vite.",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "echo \"placeholder: no test runner configured\""
  },
  "dependencies": {
    "three": "^0.170.0"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
